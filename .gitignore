# 子项目（各自有独立 git 仓库，由它们自管）
/卟卟鸡投资决策/
/方向探索/
/亚马逊投流决策助手/
/知识存储与自进化引擎/
/知识库/
/算力资源/

# 非 git 目录
/autoResearch实验室/
/Unix编程艺术/

# 一次性目录（迁移草稿）
/_migration_draft/

# MD 视觉化阅览层（mkdocs 工作区，本地起服务用，不进 git）
# 内含到各项目的 symlink + mkdocs.yml + 自动生成的 site/
/_view/

# 外部只读项目（丹丹的学术研究仓，独立 git，只读不改不推）
/丹丹知识库/

# macOS
.DS_Store
